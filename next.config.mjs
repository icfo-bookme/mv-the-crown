const nextConfig = {
    images: {
      domains: ['127.0.0.1', 'freecvbd.com', 'www.bookme.com.bd','bookme.com.bd'], // Allow these domains for external images
    }
  };
  
  export default nextConfig;
  